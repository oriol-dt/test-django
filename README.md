# 🚀 Gestor de Tareas Django

Mi primer proyecto en Django. Una aplicación CRUD completa para gestionar pendientes.

## ✨ Características
* Crear, completar y eliminar tareas.
* Interfaz responsiva con **Bootstrap 5**.
* Base de datos SQLite (desarrollo).

## 🛠️ Instalación Local
1. Clonar el repo: `git clone https://github.com/tu-usuario/tu-repo.git`
2. Crear entorno virtual: `python -m venv venv`
3. Activar entorno: `source venv/bin/activate` (o `.\venv\Scripts\activate` en Windows)
4. Instalar dependencias: `pip install -r requirements.txt`
5. Ejecutar migraciones: `python manage.py migrate`
6. Correr servidor: `python manage.py runserver`